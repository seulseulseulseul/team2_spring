<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.itwill.mapper.MapMapper">

	<select id="getAddress" resultType="com.itwill.domain.Trainer_MemberDTO">
		select t_id,t_name,t_address,t_phone,t_sns
		from trainer where t_extraAddress like CONCAT('%',#{t_extraAddress},'%')
	</select>
  
</mapper>