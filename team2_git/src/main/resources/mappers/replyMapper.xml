<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.itwill.mapper.ReplyMapper">

	<select id = "getMaxNum" resultType="java.lang.Integer">
		select max(r_num) from reply
	</select>
	
	<select id = "getMaxOrder" resultType="java.lang.Integer">
		select max(r_order) from reply where c_num=${c_num}
	</select>

	<insert id = "insertBoard">
		insert into reply(r_num,c_num,parent,r_order,depth,u_id,nic,content,date) values(#{r_num},#{c_num},#{parent},#{r_order},#{depth},#{u_id},#{nic},#{content},#{date})
	</insert>
	
	<select id = "getBoard" resultType="com.itwill.domain.ReplyDTO">
		select * from reply where r_num=#{r_num}
	</select>
	
	<select id="getBoardList" resultType="com.itwill.domain.ReplyDTO">
		select * from reply where c_num=#{c_num} order by r_order asc limit #{startRow}, #{pageSize}
	</select>
	
	<select id="numCheck" resultType="com.itwill.domain.ReplyDTO">
		select * from reply where r_num=#{r_num} and u_id=#{u_id}
	</select>
	
	<update id="updateBoard">
		update reply set content=#{content} where r_num=#{r_num}
	</update>
	
	<delete id="deleteBoard">
		delete from reply where r_num=#{r_num}
	</delete>
	
	<delete id="deleteBoardAll">
		delete from reply where c_num=#{c_num}
	</delete>
	
	<select id="getBoardCount" resultType="java.lang.Integer">
		select count(*) from reply where c_num=#{c_num}
	</select>
	
	<select id="getReplyTurn" resultType="java.lang.Integer">
		select count(*) from reply where c_num=#{c_num} and r_order &lt; #{r_order}
	</select>
	
	<update id="reOrder">
		update reply set r_order=r_order+1 where r_order &gt; #{r_order}
	</update>
	
	<select id="isNoReply" resultType="java.lang.Integer">
		select if(count(*) &gt;=1, 1, 0) from reply where parent=#{r_num}
	</select>
  
</mapper>